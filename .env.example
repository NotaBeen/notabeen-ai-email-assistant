# =============================================================================
# NotaBeen Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!

# -----------------------------------------------------------------------------
# Authentication & Security (Better Auth)
# -----------------------------------------------------------------------------
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This MUST be a 64-character hex string (32 bytes) for AES-256-GCM encryption
AUTH_SECRET=

# Optional: Separate secret for Better Auth (if not set, uses AUTH_SECRET)
BETTER_AUTH_SECRET=

# Base URL for authentication callbacks
# Development: http://localhost:3000
# Production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
AUTH_URL=http://localhost:3000/api/auth
APP_BASE_URL=http://localhost:3000/

# -----------------------------------------------------------------------------
# Google OAuth (Gmail Access)
# -----------------------------------------------------------------------------
# 1. Go to: https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Application type: Web application)
# 3. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 4. Enable Gmail API: https://console.cloud.google.com/apis/library/gmail.googleapis.com
# 5. Required scopes (configured in Better Auth):
#    - openid, profile, email
#    - https://www.googleapis.com/auth/gmail.readonly
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Database (MongoDB)
# -----------------------------------------------------------------------------
# MongoDB connection string
# Local: mongodb://localhost:27017
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=

# Database name to use
MONGO_CLIENT=notabeen

# -----------------------------------------------------------------------------
# Email Encryption
# -----------------------------------------------------------------------------
# Generate ENCRYPTION_KEY with: openssl rand -base64 24
# This produces a 32-character string for AES-256
ENCRYPTION_KEY=

# Generate ENCRYPTION_IV with: openssl rand -base64 9
# This produces a 12-character string (96 bits) for GCM mode
ENCRYPTION_IV=

# -----------------------------------------------------------------------------
# AI Engine (Google Gemini)
# -----------------------------------------------------------------------------
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=
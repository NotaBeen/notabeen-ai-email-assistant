// src/utils/text.ts

/**
 * Basic utility to clean up and format email body text.
 * This is a placeholder; implement your actual logic here.
 * @param text The raw email body text.
 * @returns The cleaned/formatted text.
 */
export function formatBodyText(text: string): string {
  // Example implementation: remove extra whitespace and newlines
  return text.replace(/\s+/g, " ").trim();
}
